<%= render "components/home/home",
           title: @title,
           type: "ferrets",
           sort: @sort,
           objects: @ferrets do %>
  <%= render partial: "components/card/card",
             collection: @ferrets,
             locals: {type: "ferrets"},
             as: :object %>
<% end %>
