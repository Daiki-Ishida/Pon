<%= render "components/home/home",
           title: @title,
           type: "ferrets",
           sort: @sort,
           objects: @ferrets do %>
  <%= render partial: "components/card/card", collection: @ferrets, as: :object %>
<% end %>
